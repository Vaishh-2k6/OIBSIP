# Backend

The backend provides REST endpoints for registration, login, dashboard access, and logout using Express and SQLite.

## Endpoints
- POST /api/register
- POST /api/login
- GET /api/dashboard
- POST /api/logout

## Setup
```bash
cd backend
npm install
npm start
```
